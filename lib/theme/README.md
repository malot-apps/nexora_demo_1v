# App Themes

Store custom palettes, light/dark style assets, and color scheme tokens here.
- `app_colors.dart` (holds Color hex code objects)
- `app_theme.dart` (compiles ThemeData details for Flutter)
- `app_typography.dart` (specifies TextTheme, fonts weights, and tracking sizes)
