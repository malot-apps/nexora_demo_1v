# IPTV Player Controls & Views

Store your video rendering widgets here. E.g.:
- `VideoPlayerContainer` (coordinates video player controllers like `video_player` or `chewie` packages)
- `PlayerControlOverlay` (overlay with seekbars, volume dials, brightness, and audio track selectors)
- `PipWindow` (Picture-In-Picture container)
