# Fonts Assets

Store custom font files (e.g., .ttf, .otf) like Space Grotesk and Inter here.
Reference them in your `pubspec.yaml` font definitions:
```yaml
flutter:
  fonts:
    - family: SpaceGrotesk
      fonts:
        - asset: assets/fonts/SpaceGrotesk-Regular.ttf
```
