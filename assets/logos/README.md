# Logos Assets

Store the official branding logos of Nexora (e.g., logo_dark.png, logo_light.png, monochrome_logo.svg) in this directory.
Reference them in `pubspec.yaml`:
```yaml
assets:
  - assets/logos/
```
